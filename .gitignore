node_modules/
flex-preview.html
# Copy-pass scratch: grounded prompts (prepare) + in-session agent answers
# (ingest). Regenerable from committed data; verified output lives in
# data/aggregates/*.json.
engine/copy-tasks/
# UI-review loop screenshots: regenerable via `npm run ui:render`. The objective
# audit + the loop history live in data/aggregates/ui-*.json.
docs/reviews/v6/shots/
